# Force HTTPS and canonical domain
http://www.postino.cc/* https://postino.cc/:splat 301!
https://www.postino.cc/* https://postino.cc/:splat 301!
http://postino.cc/* https://postino.cc/:splat 301!

# Clean URLs (optional, only if you have real fileless routes)
# Remove trailing slashes safely (you may also use Next.js trailingSlash config)
# /about/ /about 301

# Legacy redirects
/services/* /growth-marketing/:splat 301

# Let Next.js handle 404s
/* /index.html 200
